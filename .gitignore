node_modules/
dist/
.astro/
.vercel/
.env
.env.*

# Claude Code local settings
.claude/settings.local.json

# Raw, uncurated source media (may contain PII/secrets) — never publish directly.
# Curated, cleared assets are copied into assets/<project>/ instead.
/incoming/
/testimonials/
